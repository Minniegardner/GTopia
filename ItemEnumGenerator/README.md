# 🎮 Growtopia Items Enum Generator

> **Generator otomatis untuk membuat daftar item Growtopia dalam format C++**

Tool ini akan mengubah file `items.json` (format [GuckTube](https://gucktubeyt.github.io/GrowTools/) decoder) menjadi file C++ yang berisi daftar ID semua item Growtopia.

---

## 📋 Apa itu tool ini?

Tool ini adalah program yang **otomatis membuat kode C++** berisi daftar item-item Growtopia. Kamu tidak perlu menulis kode secara manual!

**Contoh hasil yang dihasilkan:**
```cpp
enum eItems {
    BLANK = 0,
    DIRT = 2,
    ROCK = 4,
    // ... dan seterusnya untuk semua item
};
```

---

## 🚀 Cara Menggunakan (Untuk Pemula)

### Langkah 1: Persiapan File

1. **Download file `items.json`** dari decoder Growtopia ([GuckTube](https://gucktubeyt.github.io/GrowTools/) format)
2. **Letakkan file `items.json`** di folder yang sama dengan project ini

### Langkah 2: Install Dependencies (Pertama Kali Saja)

**Windows:**
- Klik 2x file `install.bat`
- Tunggu sampai selesai

**Atau manual:**
- Buka terminal/command prompt
- Ketik: `npm install` lalu tekan Enter

### Langkah 3: Jalankan Generator

**Windows:**
- Klik 2x file `start.bat`

**Atau manual:**
- Buka terminal/command prompt
- Ketik: `bun start` atau `npm start` lalu tekan Enter

### Langkah 4: Hasil

✅ File `ItemsEnum.h` akan otomatis terbuat di folder project!

---

## 📦 Apa yang Dibutuhkan?

### Software yang Harus Di-install:

1. **Node.js** (versi terbaru)
   - Download: https://nodejs.org/
   - Pilih yang "LTS" (Long Term Support)

2. **Bun** (Opsional, tapi lebih cepat)
   - Download: https://bun.sh/
   - Atau gunakan Node.js saja

### File yang Dibutuhkan:

- ✅ `items.json` - File database item Growtopia (format GuckTube decoder)

---

## 📁 Struktur File Project

```
growtopia-items-enum-generator/
│
├── items.json          ← File input (kamu harus sediakan ini)
├── ItemsEnum.h         ← File output (otomatis dibuat)
│
├── install.bat         ← Klik ini untuk install (Windows)
├── start.bat           ← Klik ini untuk jalankan (Windows)
│
├── package.json        ← Konfigurasi project
├── tsconfig.json       ← Konfigurasi TypeScript
│
└── src/
    ├── main.ts         ← Program utama
    └── lib/
        └── GenerateItemsEnum.ts  ← Logic generator
```

---

## ❓ Troubleshooting (Kalau Ada Error)

### Error: "items.json not found"
**Masalah:** File `items.json` tidak ditemukan
**Solusi:** 
- Pastikan file `items.json` ada di folder yang sama dengan project
- Cek spelling nama file (harus persis `items.json`)

### Error: "npm command not found" atau "bun command not found"
**Masalah:** Node.js atau Bun belum terinstall
**Solusi:**
- Install Node.js dari https://nodejs.org/
- Atau install Bun dari https://bun.sh/

### Error: "Cannot find module" atau "Dependencies not installed"
**Masalah:** Dependencies belum di-install
**Solusi:**
- Jalankan `install.bat` atau ketik `npm install` di terminal

### File `ItemsEnum.h` tidak terbuat
**Masalah:** Ada error saat generate
**Solusi:**
- Pastikan file `items.json` valid (format JSON benar)
- Cek apakah ada pesan error di terminal
- Pastikan folder project memiliki izin write

---

## 🎯 Untuk Apa Tool Ini?

Tool ini berguna untuk:
- ✅ Developer Growtopia bot/cheat/tools
- ✅ Membuat program C++ yang perlu akses data item Growtopia
- ✅ Otomatis update enum ketika ada item baru
- ✅ Menghemat waktu menulis manual ratusan/ribuan item

---

## 🔧 Cara Kerja (Penjelasan Sederhana)

1. **Baca File:** Program membaca file `items.json`
2. **Ambil Data:** Mengambil informasi setiap item (nama dan ID)
3. **Sanitasi:** Membersihkan nama item jadi format yang valid untuk C++
4. **Generate:** Membuat kode C++ dengan format enum
5. **Simpan:** Menyimpan hasil ke file `ItemsEnum.h`

---

## 📝 Format Input (`items.json`)

File `items.json` harus punya struktur seperti ini:

```json
{
  "version": 4,
  "item_count": 2,
  "items": [
    {
      "item_id": 0,
      "name": "Blank",
      ...
    },
    {
      "item_id": 2,
      "name": "Dirt",
      ...
    }
  ]
}
```

---

## 📤 Format Output (`ItemsEnum.h`)

File yang dihasilkan akan seperti ini:

```cpp
#ifndef ITEMS_ENUM_H
#define ITEMS_ENUM_H

/* Generated by Growtopia Items Enum Generator
Version: 4, Items Count: 1234
Generated on: 2026-01-05T12:00:00.000Z
Created by Growtopia Items Enum Generator

 @see https://github.com/GTPSHAX/growtopia-items-enum-generator
*/

enum eItems {
    BLANK = 0,
    DIRT = 2,
    ROCK = 4,
    // ... semua item lainnya
};

#endif // ITEMS_ENUM_H
```

### Output Mode

Saat menjalankan generator, sekarang ada 2 mode:

1. `ItemUtils eItemID` (default)
2. `Generic eItems`

Mode default akan menghasilkan format yang kompatibel dengan `Source/Item/ItemUtils.h`, yaitu:

```cpp
enum eItemID 
{
  ITEM_ID_BLANK = 0,
  ITEM_ID_BLANK_SEED = 1,
  // ...
};
```

---

## 🤝 Kontribusi

Kalau kamu menemukan bug atau punya saran, silakan:
- Buat Issue di GitHub
- Submit Pull Request
- Hubungi developer

---

## 📜 Lisensi

Lihat file [LICENSE](/LICENSE) untuk informasi lisensi.

---

## 💡 Tips untuk Pemula

### Kalau Kamu Tidak Paham Coding:

1. **Ikuti langkah-langkah di atas dengan hati-hati**
2. **Jangan ubah file kecuali `items.json`**
3. **Kalau error, baca bagian Troubleshooting**
4. **Copy paste pesan error ke Google/ChatGPT untuk bantuan**

### Kalau Kamu Pakai AI untuk Coding:

**Prompt yang bisa kamu gunakan:**

```
"Tolong jelaskan cara menggunakan tool Growtopia Items Enum Generator ini"

"Ada error: [copy paste error] saat jalankan tool ini, bagaimana cara memperbaikinya?"

"Bagaimana cara mengintegrasikan ItemsEnum.h ke project C++ saya?"

"Tolong buatkan contoh kode C++ yang menggunakan ItemsEnum.h"
```

---

## 🔍 FAQ (Pertanyaan yang Sering Ditanya)

**Q: Apakah ini gratis?**
A: Ya, tool ini gratis dan open source.

**Q: Apakah berbahaya atau ilegal?**
A: Tidak, ini hanya tool untuk generate kode C++. Penggunaannya tergantung user.

**Q: Harus paham TypeScript/C++?**
A: Tidak! Cukup ikuti langkah-langkah di atas.

**Q: Bisa dipakai di Mac/Linux?**
A: Ya, tapi file `.bat` khusus Windows. Di Mac/Linux gunakan command manual.

**Q: Kenapa pakai Bun/Node.js?**
A: Tool ini ditulis dengan TypeScript, butuh runtime JavaScript.

**Q: Bisa edit hasil `ItemsEnum.h`?**
A: Bisa, tapi akan tertimpa kalau generate ulang. Lebih baik edit logic di `src/`.

---

## 📞 Butuh Bantuan?

Kalau masih bingung atau ada error:

1. 🔍 Baca bagian **Troubleshooting** di atas
2. 💬 Tanya ke ChatGPT/Claude/AI lain dengan copy paste error message
3. 🌐 Buat Issue di GitHub repository
4. 📧 Hubungi developer (lihat di profile GitHub)

---

**Happy Coding! 🚀**

_Tool ini dibuat untuk memudahkan developer Growtopia. Gunakan dengan bijak!_
